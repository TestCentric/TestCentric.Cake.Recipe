# This copy of GitVersion.yml is used in building the recipe package itself.
next-version: 2.0.0
mode: ContinuousDelivery
branches:
  main:
    label: dev
  release:
    label: pre
  pull-request:
    label: pr
