# This copy of GitVersion.yml is used in building the recipe package itself.
next-version: 1.4.0
mode: ContinuousDelivery
branches:
  master:
    regex: ^main$
    label: dev
    source-branches: []
  release:
    label: pre
  pull-request:
    label: pr
